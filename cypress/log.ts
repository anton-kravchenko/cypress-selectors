/*

+ 'cypress-xpath'

 +'chaining'
    + 'finds h1 within main'
    + 'finds body outside of main when succumbing to // trap'
    + 'throws when subject is more than a single element'

  + 'within()'
    + 'finds body outside of within-subject when succumbing to // trap'

  + 'logging'
    + 'should log by default'
    + 'logs the selector when not found'
    --- 'should not log when provided log: false

  + 'elements'
    + 'finds h1'
    + 'returns jQuery wrapped elements'
    + 'returns primitives as is'
    + 'gets h1 text'
    + 'retries until element is inserted'

  + 'primitives'
    + 'counts h1 elements'
    + 'returns h1 text content'
    + 'returns boolean'

  + 'typing'
    + 'works on text input'

  + 'clicking'
    + 'on button'
*/
export default 1;
